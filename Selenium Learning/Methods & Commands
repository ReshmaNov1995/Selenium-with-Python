Methods:
  get()
  find_element(By.ID, "")
  find_elements(By.XPATH, "")
  send_keys()
  click()
  clear()
  close()->will close the current browser window
  quit()->will close all the browser window
  maximize_window()

Commands:
  1) get commands
  2) conditional commands
  3)browser commands
  4) navigational commands
  5) wait commands
